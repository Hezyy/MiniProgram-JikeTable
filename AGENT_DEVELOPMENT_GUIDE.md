# 课表与日程微信小程序开发文档

> 本文档是项目的单一开发依据。接手本项目的 Agent 在开始编码前必须完整阅读本文档；若仓库中的实际实现与本文档冲突，以最新代码和本文档中的明确约束为准，并在必要时同步更新本文档。

## 1. 项目概述

开发一个以“课表查询 + 日程计划”为核心的微信小程序。产品面向需要快速查看当天课程、管理个人安排的学生。首版强调功能克制、信息清晰、视觉精致、反馈及时，同时保留增加考试、成绩、教务导入、桌面组件等能力。

产品名称：**即课课表**。

核心体验目标：

- 用户打开小程序后，3 秒内看清今天有什么课、下一节课是什么。
- 用户能按周查看完整课表，并快速切换周次。
- 用户能创建、编辑、完成和删除个人日程。
- 课程与日程使用统一的时间表达，但在视觉和数据层保持类型区分。
- 页面使用轻盈、通透、有层次的“液态玻璃”风格，同时保证文字对比度、性能和低端设备可用性。

## 2. 产品范围

### 2.1 首版必须实现

1. 首页 / 今日
   
   - 显示日期、当前教学周、星期。
   - 显示下一节课程摘要；当天无课时显示简洁空状态。
   - 按时间顺序展示当天课程和个人日程。
   - 支持完成待办型日程。
   - 提供快速新建日程入口。

2. 课表
   
   - 周视图展示周一至周日和每日时间段。
   - 支持上一周、下一周、回到本周。
   - 标识今天、当前周、已结束课程和正在进行的课程。
   - 点击课程打开详情，可新增、编辑、删除课程。
   - 支持单双周或指定周次上课。

3. 日程
   
   - 按日期分组展示日程。
   - 支持全部、待完成、已完成筛选。
   - 支持新增、编辑、完成、恢复和删除。
   - 支持普通日程和待办事项两种模式。

4. 设置
   
   - 设置学期名称、开学日期、总周数。
   - 设置周起始日，首版默认周一。
   - 数据导出、导入与清空。
   - 显示版本号和数据说明。

5. 本地数据
   
   - 首版无需登录和后端服务。
   - 使用 `wx.setStorageSync` / `wx.getStorageSync` 持久化。
   - 首次启动写入少量演示数据；用户正式编辑后不得再次自动覆盖。

### 2.2 首版不实现

- 教务系统账号登录与自动抓取。
  说明：**「账号密码代登录」永远不做**，原因见第 19.4 节；但「课表文本导入」已在首版实现，它由用户主动提供课表文本，不接触账号密码，见第 19.4 节。
- 微信云开发、多人协作、跨设备同步。
- 消息订阅提醒和系统日历同步。
- 成绩、考试、空教室、校园卡等校园服务。
- 复杂重复规则、自然语言创建日程。

这些能力只能通过预留接口和模块边界支持，不得为了未来功能提前引入复杂依赖。

## 3. 用户流程

### 3.1 首次使用

1. 启动小程序。
2. 若无本地配置，展示轻量初始化页。
3. 用户填写学期名称、开学日期和总周数，或选择“使用示例体验”。
4. 进入“今日”页。
5. 用户可从课表页添加课程，从任意主页面的新增按钮创建日程。

初始化最多一个页面，不制作营销式轮播或冗长教程。

### 3.2 查看课程

1. 用户进入课表页。
2. 默认定位本周，标题显示“第 N 周”。
3. 左右滑动或点击箭头切换周次。
4. 点击课程块，底部弹层展示课程名、教师、地点、时间、周次和备注。
5. 从详情进入编辑页。

### 3.3 创建日程

1. 点击全局新增按钮。
2. 默认创建日期为当前所浏览日期，默认开始时间为下一个整点。
3. 填写标题、日期、时间、类型、地点、备注和颜色。
4. 保存后返回来源页，并以轻提示确认。
5. 表单校验失败时，在对应字段附近提示并聚焦第一个错误字段。

## 4. 信息架构与导航

使用微信小程序原生 `tabBar`，包含 4 个标签：

| 路径                      | 标签  | 图标语义 | 职责        |
| ----------------------- | --- | ---- | --------- |
| `pages/today/index`     | 今日  | 日历日期 | 当日概览与下一节课 |
| `pages/timetable/index` | 课表  | 网格   | 周课表查询     |
| `pages/schedule/index`  | 日程  | 清单   | 日程管理与筛选   |
| `pages/settings/index`  | 设置  | 齿轮   | 学期与数据设置   |

非 Tab 页面：

- `pages/onboarding/index`：首次初始化。
- `pages/course-edit/index`：新增和编辑课程。
- `pages/event-edit/index`：新增和编辑日程。
- `pages/data-transfer/index`：数据导入与导出。

课程详情优先使用自定义底部弹层，不单独占用页面。所有页面都必须配置明确的导航栏标题。

## 5. 页面详细规格

### 5.1 今日页

从上到下包含：

1. 日期头部：大号日期、星期、学期名和当前周。
2. 下一节课程：课程名、倒计时或开始时间、地点；正在上课时显示“进行中”。
3. 今日时间线：合并课程与日程，按开始时间排序。
4. 空状态：一句状态文本和“添加日程”按钮。
5. 右下角悬浮新增按钮，使用“+”图标并带无障碍标签。

时间线条目不得嵌套多层卡片。课程条目显示颜色标识、课程名、时间、地点；日程条目额外显示完成控件。

### 5.2 课表页

- 顶部工具栏固定，包含上一周、周次标题、下一周和“回到本周”。
- 星期标题固定显示日期；今天使用高对比强调。
- 主体为横向可滚动的 7 列网格，首列为节次和时间。
- 课程块根据开始、结束节次占据对应高度，相邻课程不得重叠。
- 课程块最少显示课程名；空间足够时显示地点和教师。
- 当前时间位置可用一条细线表示，只在当前周当天显示。
- 双击、长按等隐藏手势不得作为唯一操作入口。

推荐课表布局常量：每天 8:00 至 22:00，默认 12 节。具体节次时间从设置对象读取，不写死在页面模板中。

### 5.3 日程页

- 顶部显示月份和日期选择入口。
- 使用分段控件切换“全部 / 待完成 / 已完成”。
- 内容按日期分组，组标题显示相对日期和完整日期。
- 左滑删除可以作为快捷方式，但必须在详情或编辑页提供可见删除按钮。
- 删除前弹出确认框；删除成功提供可撤销提示，撤销窗口建议 4 秒。

### 5.4 编辑表单

课程字段：

| 字段      | 必填  | 说明            |
| ------- | --- | ------------- |
| 课程名     | 是   | 1-30 个字符      |
| 教师      | 否   | 0-20 个字符      |
| 地点      | 否   | 0-30 个字符      |
| 星期      | 是   | 1-7，周一为 1     |
| 开始/结束节次 | 是   | 结束必须不早于开始     |
| 周次      | 是   | 支持全部、单双周、指定周次 |
| 颜色      | 是   | 从预设色板选择       |
| 备注      | 否   | 最多 200 字      |

日程字段：标题、日期、开始时间、结束时间、是否待办、地点、备注和颜色。结束时间必须晚于开始时间；待办允许不填写结束时间。

保存按钮固定在安全区上方。存在未保存修改时返回，必须询问是否放弃。

### 5.5 设置页

设置项按“学期”“显示”“数据”“关于”分组。每个设置项是一行，不要把每个分组做成嵌套卡片。清空数据使用危险色，需二次确认并要求明确选择。

## 6. 视觉设计规范

### 6.1 设计方向

整体采用圆角、半透明、柔和高光和细边框构成的液态玻璃风格。微信 WebView 对 `backdrop-filter` 的支持和性能可能不同，因此玻璃效果必须采用渐进增强：有能力时模糊背景，无能力时以高透明度纯色、边框和阴影保持层次。

禁止使用大面积高饱和渐变、装饰性光球、过多悬浮卡片和无意义动画。视觉服务于高频查询。

### 6.2 设计令牌

在 `styles/tokens.less` 维护统一变量或等价的公共类。微信小程序兼容性允许时使用 CSS 变量，否则由 Less 变量、Mixin 或公共类实现。

```css
page {
  --color-bg: #f4f7fb;
  --color-surface: rgba(255, 255, 255, 0.72);
  --color-surface-strong: rgba(255, 255, 255, 0.9);
  --color-text: #172033;
  --color-text-secondary: #657087;
  --color-primary: #2675ec;
  --color-success: #1f9d68;
  --color-warning: #d9822b;
  --color-danger: #d9485f;
  --color-border: rgba(82, 101, 128, 0.16);
  --radius-sm: 12rpx;
  --radius-md: 20rpx;
  --radius-lg: 28rpx;
  --shadow-soft: 0 12rpx 36rpx rgba(34, 48, 74, 0.1);
}
```

要求：

- 页面左右安全边距：`24rpx` 至 `32rpx`。
- 触控区域最小尺寸：`88rpx × 88rpx`。
- 卡片圆角不超过 `28rpx`；输入框、分段控件使用 `16rpx` 至 `20rpx`。
- 正文不小于 `28rpx`，辅助文字不小于 `24rpx`。
- 文本与背景保持清晰对比；不得仅靠颜色传达状态。
- 课程预设色至少 8 种，保持浅背景配深文字，并为颜色提供名称。
- 适配状态栏、胶囊按钮、底部安全区和不同屏幕宽度。

玻璃面板建议样式：

```css
.glass-panel {
  background: rgba(255, 255, 255, 0.72);
  border: 1rpx solid rgba(255, 255, 255, 0.78);
  box-shadow: 0 12rpx 36rpx rgba(34, 48, 74, 0.1);
  backdrop-filter: blur(20rpx) saturate(130%);
  -webkit-backdrop-filter: blur(20rpx) saturate(130%);
}
```

### 6.3 图标与插图

- 优先使用统一风格的本地图标资源或成熟的小程序图标组件。
- 常见操作用图标表达：返回、关闭、编辑、删除、切换周次、新增。
- 图标按钮必须设置 `aria-label` 或可理解的辅助文本。
- 空状态可使用简洁插图，但不得依赖远程图片，首包资源需控制体积。

## 7. 动效与反馈

- 点击：元素轻微缩小到 `0.97`，持续 100-140ms；抬起恢复。
- 页面内容：首次进入可做 160-220ms 的淡入和轻微上移。
- 底部弹层：使用 220-280ms 的缓动进入，遮罩同步淡入。
- 保存：按钮进入加载状态并防止重复提交，成功后轻提示并返回。
- 失败：保留用户输入，显示可操作的错误原因。
- 完成待办：勾选状态即时变化，可配合轻触觉反馈 `wx.vibrateShort`，但需捕获失败且不影响主流程。
- 切换周次时保持工具栏稳定，避免整体布局跳动。
- 尊重系统减少动态效果的偏好；无法检测时也要控制幅度和时长。

所有异步操作必须有加载、成功、失败三种状态。超过 400ms 的操作才显示明显加载，避免闪烁。

## 8. 技术方案

### 8.1 技术栈

- **创建微信小程序项目时，必须在微信开发者工具中选择 `TypeScript + Less` 模板。不要选择 JavaScript、基础模板或其他模板。**
- 微信小程序原生框架：WXML、TypeScript、Less。
- 业务逻辑统一使用 TypeScript，核心模型集中定义为 `interface` 或 `type`，禁止退回 JavaScript + JSDoc 的实现方式。
- 样式源文件统一使用 `.less`；由微信开发者工具负责模板自带的编译流程，不额外引入重复的 Less 构建链。
- 最低基础库版本在 `project.config.json` 和 README 中说明，开发时以当前稳定基础库为目标。
- 除确有必要，不引入 UI 框架；公共组件应由项目自身实现。

### 8.2 推荐目录

```text
miniprogram/
├─ app.ts
├─ app.json
├─ app.less
├─ assets/
│  ├─ icons/
│  └─ images/
├─ components/
│  ├─ glass-panel/
│  ├─ empty-state/
│  ├─ segmented-control/
│  ├─ course-card/
│  ├─ timeline-item/
│  ├─ bottom-sheet/
│  └─ toast/
├─ pages/
│  ├─ today/
│  ├─ timetable/
│  ├─ schedule/
│  ├─ settings/
│  ├─ onboarding/
│  ├─ course-edit/
│  ├─ event-edit/
│  └─ data-transfer/
├─ services/
│  ├─ storage.ts
│  ├─ course-service.ts
│  ├─ event-service.ts
│  └─ export-service.ts
├─ store/
│  └─ app-store.ts
├─ styles/
│  ├─ tokens.less
│  └─ utilities.less
├─ types/
│  └─ models.ts
└─ utils/
   ├─ date.ts
   ├─ id.ts
   ├─ validation.ts
   └─ timetable.ts
```

页面负责展示和用户事件，业务逻辑放在 `services`，纯计算放在 `utils`。禁止页面直接散落读写 Storage 的代码。

### 8.3 状态管理

首版使用轻量单例 Store：

- 保存当前学期、课程、日程和 UI 级筛选状态。
- 提供 `getState`、`setState`、`subscribe`、`unsubscribe`。
- Service 完成数据变更后统一更新 Store 并持久化。
- 页面在 `onLoad` / `onUnload` 中订阅和取消订阅，避免泄漏。

不得为了首版引入 Redux 类库。

## 9. 数据模型

所有日期使用本地日期字符串 `YYYY-MM-DD`，时间使用 `HH:mm`。时间戳用于 `createdAt` 和 `updatedAt`。生成 ID 时使用时间戳加随机字符串，禁止依赖数组下标。

```ts
interface AppData {
  version: 1;
  initialized: boolean;
  semester: Semester | null;
  courses: Course[];
  events: ScheduleEvent[];
  preferences: Preferences;
}

interface Semester {
  id: string;
  name: string;
  startDate: string;
  totalWeeks: number;
  classTimes: Array<{ section: number; start: string; end: string }>;
}

interface Course {
  id: string;
  name: string;
  teacher: string;
  location: string;
  weekday: 1 | 2 | 3 | 4 | 5 | 6 | 7;
  startSection: number;
  endSection: number;
  weeks: number[];
  colorId: string;
  note: string;
  createdAt: number;
  updatedAt: number;
}

interface ScheduleEvent {
  id: string;
  title: string;
  date: string;
  startTime: string;
  endTime: string;
  kind: 'event' | 'todo';
  completed: boolean;
  location: string;
  note: string;
  colorId: string;
  createdAt: number;
  updatedAt: number;
}

interface Preferences {
  weekStartsOn: 1;
  theme: 'system' | 'light' | 'dark';
  hapticsEnabled: boolean;
}
```

本地存储主键：`timetable_app_data_v1`。导出文件必须包含 `schemaVersion`、`exportedAt` 和 `data`。导入时先解析、校验、预览统计，再由用户确认覆盖；不可信字段不得直接渲染为富文本。

### 9.1 日期与周次规则

- `startDate` 所在周为第 1 周，周一为一周开始。
- 当前周：计算目标日期所在周的周一，与开学周周一之间的整数周差再加 1。
- 早于开学日期或晚于总周数时显示“非教学周”。
- 课程仅在 `weeks` 包含目标周且 `weekday` 匹配时出现。
- 所有日期计算集中在 `utils/date.ts` 并为跨月、跨年、闰日和夏令时边界编写测试。

## 10. 组件规范

每个公共组件必须：

- 使用清晰的 `properties`、`data` 和自定义事件命名。
- 不直接访问全局 Store，数据由页面传入，操作通过事件上抛。
- 包含默认态、按下态、禁用态；涉及异步时包含加载态和失败态。
- 支持长标题截断或换行，不能撑破布局。
- 避免在 `setData` 中传输大对象，只传视图需要的数据。

关键事件使用动词：`tapcourse`、`changefilter`、`confirm`、`cancel`、`retry`。组件对外数据结构写入组件目录的注释或 README。

## 11. 数据服务接口

Service 至少暴露以下接口，具体可按实现调整，但调用者不应直接修改原始数组：

```js
courseService.listByWeek(weekNumber)
courseService.listByDate(date)
courseService.getById(id)
courseService.create(input)
courseService.update(id, patch)
courseService.remove(id)

eventService.listByDate(date)
eventService.listRange(startDate, endDate)
eventService.create(input)
eventService.update(id, patch)
eventService.toggleComplete(id)
eventService.remove(id)

storageService.load()
storageService.save(appData)
storageService.migrate(rawData)
storageService.clear()
```

所有写操作先校验、再生成新数据、持久化成功后更新界面状态。Storage 写入失败时必须提示，不能假装保存成功。

## 12. 异常与边界情况

必须处理：

- 学期尚未配置、课程或日程为空。
- 同一时间存在多个课程或日程，列表中均显示；课表网格允许并排或提示冲突。
- 课程跨多个节次、标题很长、地点缺失。
- 日期跨月、跨年以及设备时间变化。
- 本地数据损坏、版本落后、导入数据字段缺失。
- Storage 空间不足或写入失败。
- 用户快速重复点击保存、删除、切换周次。
- iPhone 安全区、Android 小屏和大字号模式。

发现损坏数据时先保留原始数据备份，再尝试迁移；无法恢复则提示用户导出诊断数据或重置。不要静默清空。

## 13. 性能与可访问性

- 首屏只计算当天和当前周数据，不预生成整个学期视图。
- 大列表使用分页、分段加载或虚拟列表；首版至少避免一次渲染全学期所有事件。
- 减少 `setData` 的数据量和调用次数。
- 图片使用合适尺寸并压缩，避免远程首屏依赖。
- 交互控件提供可读标签，装饰图标对辅助技术隐藏。
- 支持系统字体缩放，关键文本不使用固定高度裁切。
- 焦点顺序与视觉顺序一致，弹层关闭后尽量恢复触发点。

## 14. 隐私与安全

- 首版只存储用户主动输入的课程与日程，不申请位置、通讯录等无关权限。
- 不上传任何数据，不接入统计 SDK。
- 导出内容可能包含私人日程，导出前提示数据范围。
- 所有导入数据都视为不可信：校验类型、长度、枚举、日期和数组规模。
- 页面中禁止使用 `rich-text` 渲染未经清洗的用户输入。

## 15. 开发顺序

1. 初始化原生小程序结构、全局样式和设计令牌。
2. 实现数据模型、Storage、迁移、日期工具和测试。
3. 完成初始化页与学期设置。
4. 完成课程增删改查和课表周视图。
5. 完成日程增删改查、筛选与完成状态。
6. 完成今日页的课程与日程合并时间线。
7. 完成导入、导出、清空和异常恢复。
8. 统一动效、触觉反馈、空状态和错误状态。
9. 在微信开发者工具中进行真机与多尺寸验收。

每一步完成后保持项目可运行，避免一次提交大量未接通的空页面。

## 16. 验收标准

### 16.1 功能验收

- 首次启动能完成学期初始化并进入今日页。
- 可创建跨不同周次和节次的课程，切换周次后显示正确。
- 单双周或指定周次课程只在对应周出现。
- 可创建普通日程和待办，刷新或重启后数据仍存在。
- 待办完成、恢复、编辑、删除行为正确。
- 今日页能按时间合并课程与日程，并正确识别下一节课。
- 导出的数据可以重新导入，非法文件不会破坏现有数据。
- 清空操作需要确认，完成后回到合理的初始化状态。

### 16.2 视觉与交互验收

- iPhone SE 等窄屏无横向溢出，课表自身的受控横向滚动除外。
- 主流 iPhone 和 Android 模拟器中无遮挡、错位和底部安全区问题。
- 玻璃效果关闭或不支持时，层级和文字仍清晰。
- 所有可点击元素均有按压反馈，保存等异步操作不会重复触发。
- 空、加载、失败、禁用、完成和冲突状态均有明确展示。
- 长课程名、长地点和系统大字号不会造成文字覆盖。

### 16.3 质量验收

- 日期、周次、课程筛选和数据迁移的核心函数有单元测试。
- 微信开发者工具控制台无错误和持续警告。
- 不存在页面直接读写 Storage、复制粘贴日期算法或未清理的定时器。
- 发布构建不包含调试日志、测试数据开关或敏感信息。

## 17. 后续扩展约定

未来功能通过独立 Service 和页面接入：

- 教务导入：实现 `ImportProvider`，转换为统一 `Course`，不得让课表页依赖具体学校格式。
  **已实现**：`utils/schedule-parser.ts` 是纯函数解析层（`ImportProvider`），`services/course-import-service.ts` 负责转换与写入，`pages/course-import/` 是入口页面。新增学校只需在解析层追加格式分支，课表页与数据模型不需要改动。见第 19.4 节。
- 云同步：在 Storage Service 之上增加 Repository 层，页面接口保持不变。
- 提醒：为日程增加可选 reminder 配置，并由独立提醒服务处理授权。
- 考试与成绩：新增领域模型和独立页面，可在今日时间线中通过适配器聚合。
- 主题：从设计令牌切换，组件不得硬编码颜色。
- 多学期：将当前单个 `semester` 扩展为 `semesters` 与 `activeSemesterId`，课程预留按学期过滤能力。

数据结构每次破坏性变更都必须提升 `version`，提供迁移函数，并保留至少上一个版本的迁移路径。

## 18. Agent 执行要求

接手开发的 Agent 应按以下方式工作：

1. 先检查仓库现状、`AGENTS.md`、配置和已有代码，再确定需要实现的阶段。
2. 遵循本文档的产品边界，不擅自加入登录、后端或大型框架。
3. 优先完成可运行的端到端流程，再补充非关键视觉细节。
4. 修改数据模型、目录结构、核心交互或验收规则时同步更新本文档。
5. 每次改动后运行相关测试，并在微信开发者工具中检查页面；涉及布局时至少验证窄屏和常规屏幕。
6. 对无法自动验证的微信能力明确记录手工验证步骤。
7. 使用真实可读的中文界面文案，避免“功能介绍”“点击这里”等开发说明出现在产品界面。

首版完成的定义：用户能从空数据完成初始化，添加课程和日程，在今日与课表中正确查看，并在重启后保留数据；核心异常有反馈，主要屏幕下的布局达到可发布质量。

## 19. 实现现状与文档同步（v1.0.0）

本节记录首版实现相对上文规格的**补充与偏差**，属于本文档的一部分。

### 19.1 目录结构补充

| 新增文件                       | 职责                                                             |
| -------------------------- | -------------------------------------------------------------- |
| `components/icon/`         | 统一风格的图标组件；图标为内联 SVG data URI，由脚本生成，不依赖远程资源                   |
| `utils/schema.ts`          | 存储主键、结构版本、迁移链、不可信数据清洗与导出负载；纯函数，可在 Node 中直接测试                  |
| `utils/colors.ts`          | 9 种预设色（浅背景 + 深文字 + 强调色 + 名称）                                   |
| `utils/theme.ts`           | 主题解析（跟随系统）与导航栏 / 窗口背景适配                                        |
| `utils/feedback.ts`        | 轻触觉反馈与自定义轻提示封装                                                 |
| `utils/dialog.ts`          | `wx.showModal` 的 Promise 化确认框，以及「未保存修改」离开确认                     |
| `utils/undo.ts`            | 删除后的撤销快照（WeakMap 绑定页面实例，不在 `setData` 中传大对象）                     |
| `utils/timers.ts`          | 定时器注册与统一清理，杜绝未清理的定时器                                           |
| `utils/logger.ts`          | 分级日志；调试与警告日志在正式版静默，错误日志始终输出                                     |
| `utils/schedule-parser.ts` | 教务课表文本解析（ImportProvider）；纯函数，可单元测试                           |
| `services/course-import-service.ts` | 把解析结果收敛到当前学期、分配颜色、去重并写入 Store                        |
| `pages/course-import/`     | 粘贴课表、预览识别结果、选择追加或覆盖并导入                                   |
| `tools/generate-icons.mjs` | 生成 tabBar PNG 图标与 `styles/icons.less`（零依赖）                       |
| `tools/check-less.mjs`     | 用官方 less 编译器逐个校验所有 `.less` 文件                                 |
| `tools/check-press-feedback.mjs` | 强制所有可点击元素都有按压反馈，并禁止触觉反馈调用                            |

`styles/` 拆分为三个文件：`variables.less`（Less 变量与混入，经 `lessSetting.commonUseFilePath` 公共引用且不产生 CSS 输出）、`tokens.less`（CSS 自定义属性设计令牌，只在 `app.less` 引入一次）、`utilities.less`（全局工具类）。`icons.less` 由脚本生成。

### 19.2 与规格的偏差

1. **`store/app-store.ts` 额外导出 `commitAppData(next)`**：数据写入的唯一出口，先持久化成功再更新 Store。Service 只调用它，页面不直接读写 Storage。
2. **`AppData.version` 为 `number`**（而非字面量 `1`），便于与迁移链比较；`SCHEMA_VERSION = 1` 表示当前版本。
3. **`Preferences.weekStartsOn` 取值 `1 | 7` 且在设置页可切换**。第 9.1 节「周一为一周开始」仍是默认行为；切换为周日后，课表列顺序、周次计算与日期换算整体轮转，并有单元测试覆盖。
4. **第 9.1 节周次规则的实现**：以 `startDate` 所在周的起始日与目标周的起始日求整天差，除以 7 再加 1，因此开学日期不在周一时仍能得到正确周次。
5. **新增入口**：今日页与日程页使用右下角悬浮按钮，课表页工具栏提供「+ 课程」。课表页的左右滑动切换周次未实现——课表主体本身横向滚动，滑动会与之冲突；工具栏箭头与「回到本周」已满足「隐藏手势不得作为唯一操作入口」。
6. **日程删除入口**：编辑页始终提供可见的删除按钮；长按列表项弹出操作表作为快捷方式。第 5.3 节的左滑删除为可选项，首版未实现。
7. **提示策略**：编辑页保存成功使用原生 `wx.showToast`（可跨页面返回后继续显示）；其余场景统一使用 `components/toast`，该组件支持「撤销」操作按钮（默认 4 秒）。
8. **主题**：`app.json` 开启 `darkmode` 并配置 `theme.json`，用于读取系统主题与原生导航栏 / 窗口配色；页面根节点使用 `theme-light` / `theme-dark` 切换设计令牌。课程预设色在深色主题下仍为浅底深字，以保证对比度。
9. **课表网格采用「一页流」布局（对第 5.2 节的重要调整）**：星期一至星期日 **7 列全部在屏幕内**，任何情况下都不横向滚动；页面上只有纵向滚动。与之配套的实现：
   - 首列（节次与时间）宽度固定 `64rpx`，其余 7 列由 Flex 均分（750rpx 屏宽下约 `95rpx`/天），列宽写在 `pages/timetable/index.less`，不再由 JS 计算，因此删除了 `TIME_COLUMN_WIDTH` / `DAY_COLUMN_WIDTH` / `GRID_CONTENT_WIDTH` 三个常量；
   - 工具栏、冲突提示与星期标题同一个 `position: sticky` 容器吸附在顶部，纵向滚动时始终可见且天然对齐；
   - 节次仍为等高行（`SECTION_HEIGHT = 112rpx`）+ 午休 / 晚间休息行（`BREAK_HEIGHT = 48rpx`），总高 `1440rpx`；课程块按起止节次的累计位置定位，重叠课程按「重叠簇」分列并排显示；
   - 窄列适配：课程块左边条收窄到 `6rpx`、字号降到 `20rpx`；并排冲突使列宽低于 60% 时进入 `is-narrow` 形态（字号 `16rpx`、只显示课程名）；「进行中」「冲突」角标在窄块中隐去，分别改由主色内描边与危险色左边条表达，冲突同时保留顶部文字提示。
10. **性能边界**：今日页只计算当天数据；课表页只计算当前周；日程页使用滚动时间窗口（初始 -30 ~ +90 天，每次「加载更多」+90 天，上限 730 天）；导入规模限制为课程 ≤ 500、日程 ≤ 2000。
11. **渲染器**：关闭 Skyline（`skylineRenderEnable: false` 并移除 `rendererOptions`），使用 WebView 渲染，以保证 `backdrop-filter` 渐进增强与 `position: sticky` 行为在各类设备上可预期。
12. **填充色与语义色分离**：设计令牌在语义色（`--color-primary` / `--color-success` / `--color-danger`，用于文字与描边）之外，增加承载白色内容的填充色 `--color-primary-strong` / `--color-success-strong` / `--color-danger-strong` 以及 `--color-on-strong`。深色主题下语义色会被提亮以保证小字对比度，若直接用作填充底色会让白色内容对比不足，因此两者必须分开使用。
13. **显式 `@import` 优先**：每个 `.less` 文件都显式 `@import` 设计令牌（变量与混入），不依赖 `lessSetting.commonUseFilePath` 的隐式注入；该设置仍指向 `styles/variables.less` 作为兜底，但即使被忽略也能正常编译。
14. **不使用触觉（震动）反馈**：第 7 节中「可配合轻触觉反馈 `wx.vibrateShort`」一条**不实现**。所有操作反馈只由可视化的按压态与轻提示承担，产品中不存在任何震动调用。因此 `Preferences` 移除了 `hapticsEnabled`，数据结构版本提升到 **v2**，并提供 v1 → v2 迁移函数（`MIGRATIONS[1]`）删除该字段；v0 → v1 → v2 的迁移路径完整保留。
15. **统一的按压反馈实现**：全站唯一的按压态是 `.jk-pressed`（`transform: scale(0.97)` + `opacity: 0.9`，过渡 120ms 写在按下态规则内），由 `hover-class="jk-pressed"` 触发。把过渡写在按下态里，使得任何可点击元素只要挂上该 class 就自动获得动画，不需要逐个修改基础样式。较小或形态特殊的控件使用自己的按下态（`.tl__check--pressed`、`.seg__item--pressed`、`.toast__action--pressed`）。四个自定义组件的样式表内再次声明 `.press-class()`，因此按压反馈不依赖全局样式的注入。`<switch>` 等原生控件保留其自带反馈。

### 19.3 验证方式

- `npm run verify`：`tsc --noEmit` 类型检查 + Node 内置测试运行器的单元测试 + 官方 `less` 编译器对全部 `.less` 文件的语法与引用校验 + 按压反馈覆盖检查。
- 单元测试覆盖：日期与周次计算（跨月、跨年、闰日、夏令时、周日作为一周起始、开学日不在周一）、节次定位与冲突分列、当前时间线与下一节课识别、字段校验规则、存储迁移（含 v1 → v2 迁移链）与导入数据清洗、教务课表文本解析（网格与逐行两种格式、单双周、连堂合并、一格多课、无法识别的输入）。
- `tools/check-press-feedback.mjs` 遍历全部 WXML：带 `bindtap` / `catchtap` / `bindlongpress` 的原生元素必须声明 `hover-class`，`<picker>` 的直接子元素必须声明 `hover-class`，任何源码文件都不得出现 `wx.vibrateShort` / `wx.vibrateLong`。自定义组件的按压态在其自身模板中接受同一套检查。
- 无法自动验证的微信能力（安全区、玻璃降级、深色主题、实际动画手感等）逐条列在 `README.md` 的手工验收清单中。

### 19.4 教务课表导入（ImportProvider）

**为什么不做账号密码代登录。** 用户提出的原始需求是「输入统一身份认证账号密码自动获取课表」。这条路径在本项目中永久排除，理由有三，且互相独立：

1. **学校明文禁止。** 福建理工大学教务处 2025-05-26《关于规范教学信息查询渠道的通知》点名「第三方平台（包括但不限于课表查询类 APP、小程序等）……学生或老师登录第三方平台也会有信息泄露等风险，学校不承担由此产生的任何责任」。官方唯一渠道是正方教务系统（`jwxt.fjut.edu.cn`）。
2. **小程序端技术上不可靠。** `wx.request` 只能请求公众平台配置过的服务器域名，开发者工具里「不校验合法域名」只在本地模拟器有效；正方教务的登录走统一身份认证多跳重定向，`wx.request` 不暴露中间跳转的响应头，难以稳定取得 CAS ticket 与 `JSESSIONID`；再叠加验证码、密码加密（公钥轮换即失效）与会话保持，真机成功率低且学校改版即失效。`web-view` 也走不通——业务域名需要在对方域名根目录放校验文件。
3. **安全责任落在开发者身上。** 让用户把统一身份认证密码输入第三方小程序，是真实的钓鱼式风险面，且微信审核对「代登录学校系统 + 收集账号密码」高度敏感。

**采用的方案：粘贴导入。** 用户从教务系统复制课表文本，粘贴进小程序，本地解析成 `Course`。不接触账号密码、不发起任何网络请求、数据不出本机。

实现分层（对应第 17 节的 `ImportProvider` 约定）：

| 层 | 文件 | 职责 |
| --- | --- | --- |
| 解析（纯函数，可测） | `utils/schedule-parser.ts` | 识别文本格式 → 输出 `ParsedCourse[]` + `issues` + `warnings` |
| 转换与写入 | `services/course-import-service.ts` | 收敛到当前学期、分配颜色、去重、生成预览、写入 Store |
| 页面 | `pages/course-import/` | 粘贴 / 读取剪贴板 / 预览 / 选择追加或覆盖 / 确认导入 |

解析器支持的两种来源格式（自动识别）：

- **`grid`**：浏览器复制课表表格产生的文本。单元格以 `\t` 分隔，`\n` 既可能是行分隔也可能是单元格内换行——行首一定是节次标签，因此把「`\n` + 节次标签」统一当作行分隔。**按表头列下标定位星期**，而不是按「第几个非空单元格」，这样空单元格与时间列都不会让后面的星期整体错位。
- **`line`**：每行一条完整课程记录（教务系统的「课程列表」视图），行内包含星期与节次。

已覆盖的写法：`1-16周`、`1-16周(单)`、`1,3,5-8周`、**`1-12周,14-16周`（正方多段周次）**、`第3-5周`、`单周`、`3-5`；`第1-2节`、`3-4节`、`第5节`；`★` 与 `@` 分隔的紧凑单元格；一格多课；**同格多时段继承课程名（正方 V9.0 行为）**；教师/地点前缀（`教师：`、`地点：`）。

针对正方教务「按周次查询」页面（`xskbcxZccx_cxXskbcxIndex`）的专门处理：该页面会把「第 N 周」渲染成独立元素，整页复制时会混入解析流程。解析器把整格等于「第 N 周」的内容识别为表头标记并跳过，避免被误读成「只上第 N 周」。

已知限制与处理：

- 教务系统用**合并单元格**表示连堂，复制成纯文本后信息会丢失。解析器会把「同一天、相邻节次、课程名/教师/地点/周次完全相同」的课程合并为一个课程块，并在 `issues` 中提示「检测到 N 处连堂，已按相邻节次合并，请核对节次」，让用户知道需要确认。
- 原文没有标注周次的课程按**全部周次**导入，并在 `warnings` 中汇总提示。
- 解析不出的单元格会连同原始文本记录在 `issues` 中，页面直接展示，便于排查与后续改进解析器。
- 若解析出的最大周次超过当前学期总周数，页面会建议并允许用户一并扩展学期总周数。
